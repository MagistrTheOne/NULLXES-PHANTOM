# Manifests

This directory is the provenance layer for `PHANTOM`.

Use it for:

- source manifests
- corpus manifests
- license manifests
- run manifests

If a training run cannot be reconstructed from manifests, it is not production-grade.
