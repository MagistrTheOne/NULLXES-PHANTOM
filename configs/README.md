# Configs

This directory will hold frozen machine-readable configuration trees for:

- `model/`
- `data/`
- `train/`
- `eval/`
- `serve/`

Config files should be treated as versioned contracts. Large runs must reference immutable config snapshots.
